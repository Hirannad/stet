name: validate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Structural checks
        run: python3 scripts/check.py

      # No-op until tests/corpus/runs/ has something in it. Once it does, a recorded run that
      # drifts from the shape SKILL.md specifies fails here instead of being found by hand.
      - name: Recorded run shapes
        run: make runs

      - name: Agent Skills discovery
        run: npx --yes skills@1.5.20 add . --list

      - name: Plugin and marketplace manifests
        run: |
          npm install --global @anthropic-ai/claude-code@2.1.229
          claude plugin validate . --strict
